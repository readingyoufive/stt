Generated by GitHub Actions.

This directory receives:
- encoder.int8.onnx
- decoder.int8.onnx
- joiner.int8.onnx

They are served as separate static files and mounted through WORKERFS in
public/parakeet-worker.js instead of being copied into Emscripten MEMFS/.data.
