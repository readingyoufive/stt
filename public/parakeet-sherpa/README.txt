Generated by GitHub Actions.

v10 runtime layout:
- sherpa-onnx JS/WASM
- a SMALL .data containing tokens.txt, bpe.vocab and silero_vad.onnx only

The large Parakeet ONNX files are published separately under /parakeet-model/
and mounted read-only in the Worker through Emscripten WORKERFS.
