STT v10.7 WORKERFS + DIAGNOSTICS — ARCHIVE COMPLETE

1. Décompresser l'archive.
2. Remplacer le contenu du dépôt GitHub par TOUT le contenu décompressé, y compris le dossier caché .github.
3. Vérifier que le seul workflow est .github/workflows/pages.yml.
4. Commit sur main.
5. Dans Actions, le log doit afficher :
   WORKFLOW_REVISION=v10.7-workerfs-diagnostics
6. Attendre que build ET deploy soient verts.
7. Recharger la page. L'en-tête doit afficher v10.7 et le journal doit recevoir :
   Worker v10.7-workerfs-diagnostics

La clé runtime est workerfs-v10.7, donc le runtime sherpa est recompilé une fois.
Aucun fichier Emscripten .data n'est généré ou publié.
