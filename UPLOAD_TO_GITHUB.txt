STT v10.4 WORKERFS — ARCHIVE COMPLETE

1. Dézipper cette archive.
2. Remplacer le contenu de ton dépôt par TOUT le contenu extrait.
3. IMPORTANT : vérifier que ce fichier existe sur GitHub :
   .github/workflows/pages.yml
4. Il ne doit PAS y avoir de pages.yml à la racine du dépôt.
5. Commit/push sur main.
6. GitHub > Settings > Pages > Source : GitHub Actions.
7. GitHub > Actions > attendre que "Build STT demo and deploy Pages" soit entièrement vert.

Cette v10.4 force une nouvelle clé de cache : workerfs-v10.4.
Le workflow exporte FS uniquement et lie WORKERFS avec -lworkerfs.js.
WORKERFS est utilisé côté navigateur via FS.filesystems.WORKERFS.

Le build refuse de déployer si :
- WORKERFS n'est pas présent dans le runtime généré ;
- le .data dépasse 50 MB ;
- les poids ONNX ont été ré-embarqués accidentellement dans le .data.
